import { CanUploadPipe } from './can-upload.pipe';

describe('CanUploadPipe', () => {
  it('create an instance', () => {
    const pipe = new CanUploadPipe();
    expect(pipe).toBeTruthy();
  });
});
