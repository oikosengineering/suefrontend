import { DocumentsUploadedPipe } from './documents-uploaded.pipe';

describe('DocumentsUploadedPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentsUploadedPipe();
    expect(pipe).toBeTruthy();
  });
});
