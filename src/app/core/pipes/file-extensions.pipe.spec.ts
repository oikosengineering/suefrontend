import { FileExtensionsPipe } from './file-extensions.pipe';

describe('FileExtensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
