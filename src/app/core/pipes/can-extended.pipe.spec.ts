import { CanExtendedPipe } from './can-extended.pipe';

describe('CanExtendedPipe', () => {
  it('create an instance', () => {
    const pipe = new CanExtendedPipe();
    expect(pipe).toBeTruthy();
  });
});
