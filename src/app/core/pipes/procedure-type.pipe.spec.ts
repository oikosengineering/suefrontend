import { ProcedureTypePipe } from './procedure-type.pipe';

describe('ProcedureTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcedureTypePipe();
    expect(pipe).toBeTruthy();
  });
});
